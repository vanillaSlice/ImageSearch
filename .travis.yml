language: python
branches:
  only:
    - master
python: 3.7
dist: xenial
sudo: true
install:
  - pip install -r requirements.txt
  - pip install pylint
  - pip install pytest-cov
  - pip install coveralls
script:
  - pylint imagesearch/
  - pytest --cov=imagesearch/
after_success:
  - coveralls
